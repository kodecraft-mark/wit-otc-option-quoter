package jabratech:component-garman-kohlhagen@0.1.2;

world component-garman-kohlhagen {
    
    export calculate-call: func(
        spot: f64, 
        strike: f64, 
        ttm: f64, 
        r2: f64, 
        r1: f64, 
        iv: f64
    ) -> result<f64, string>;

    export calculate-put: func(
        spot: f64, 
        strike: f64, 
        ttm: f64, 
        r2: f64, 
        r1: f64, 
        iv: f64
    ) -> result<f64, string>;

}